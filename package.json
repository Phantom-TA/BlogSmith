{
  "name": "blogsmith-monorepo",
  "version": "1.0.0",
  "description": "Unified build for BlogSmith",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-app": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.1"
  }
}
